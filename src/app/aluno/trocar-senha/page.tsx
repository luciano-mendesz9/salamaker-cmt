"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {LockKeyhole} from "lucide-react";import {toast} from "sonner";import {Brand} from "@/components/brand";
export default function TrocarSenha(){const router=useRouter();const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const form=new FormData(e.currentTarget);const response=await fetch("/api/auth/change-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({password:form.get("password"),confirmation:form.get("confirmation")})});const data=await response.json();setLoading(false);if(!response.ok){toast.error(data.message);return}toast.success("Senha criada. Entre novamente.");router.replace(data.redirectTo)}return <main className="mesh grid-pattern grid min-h-screen place-items-center px-5"><div className="w-full max-w-md"><div className="mb-8 flex justify-center"><Brand context="Estudante"/></div><section className="glass rounded-3xl p-7"><LockKeyhole className="text-blue-300"/><h1 className="mt-5 font-display text-3xl font-bold">Crie sua nova senha</h1><p className="mt-2 text-sm text-slate-400">A senha temporária precisa ser substituída antes de acessar o painel.</p><form onSubmit={submit} className="mt-7 space-y-4"><input name="password" type="password" className="input" placeholder="Nova senha (mínimo de 8 caracteres)" minLength={8} required/><input name="confirmation" type="password" className="input" placeholder="Confirme a nova senha" minLength={8} required/><button disabled={loading} className="button-primary w-full">{loading?"Salvando...":"Criar nova senha"}</button></form></section></div></main>}
