import {NotificationManager} from "@/components/notification-manager";import {ProfessorShell} from "@/components/professor-shell";import {requireTeacher} from "@/lib/authorization";import {prisma} from "@/lib/db";import {deleteExpiredPopups} from "@/lib/notifications";
export default async function Notificacoes(){await requireTeacher();await deleteExpiredPopups();const notifications=await prisma.notification.findMany({include:{activity:{select:{title:true}}},orderBy:{createdAt:"desc"}});const rows=notifications.map(item=>({...item,expiresAt:item.expiresAt?.toISOString()??null,createdAt:item.createdAt.toISOString(),activityTitle:item.activity?.title??null}));return <ProfessorShell title="Notificações"><p className="eyebrow">Comunicação</p><h2 className="mt-3 font-display text-3xl font-bold">Notificações dos alunos</h2><p className="mt-2 max-w-3xl text-sm text-slate-400">Publique avisos na caixa de entrada ou como pop-up no primeiro acesso diário. Somente o professor pode remover as mensagens globais.</p><NotificationManager initialNotifications={rows}/></ProfessorShell>}
