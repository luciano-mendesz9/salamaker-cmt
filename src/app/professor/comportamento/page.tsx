import {ProfessorShell} from "@/components/professor-shell";import {BehaviorManager} from "@/components/behavior-manager";import {prisma} from "@/lib/db";
export default async function Comportamento(){const rows=await prisma.lesson.findMany({where:{status:"CLOSED"},select:{id:true,title:true,closedAt:true,attendances:{select:{student:{select:{id:true,firstName:true,lastName:true}}}},behaviors:{select:{id:true,studentId:true,score:true,reason:true,updatedAt:true}}},orderBy:{closedAt:"desc"},take:50});const lessons=rows.map(l=>({id:l.id,title:l.title,closedAt:l.closedAt?.toISOString()??null,students:l.attendances.map(a=>a.student),ratings:l.behaviors.map(r=>({...r,updatedAt:r.updatedAt.toISOString()}))}));return <ProfessorShell title="Comportamento"><p className="eyebrow">Acompanhamento respeitoso</p><h2 className="mt-3 font-display text-3xl font-bold">Avaliação por aula</h2><p className="mt-2 text-sm text-slate-400">Avalie alunos de aulas fechadas. Correções revertem o XP anterior antes de aplicar o novo valor.</p><BehaviorManager lessons={lessons}/></ProfessorShell>}
