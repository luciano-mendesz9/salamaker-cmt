import {ProfessorShell} from "@/components/professor-shell";import {SettingsPanel} from "@/components/settings-panel";import {prisma} from "@/lib/db";import {requireTeacher} from "@/lib/authorization";
export default async function Moderacao(){await requireTeacher();const settings=await prisma.appSetting.upsert({where:{id:1},create:{id:1},update:{}});return <ProfessorShell title="Moderação e tema"><SettingsPanel initial={{accentColor:settings.accentColor,studentAreaEnabled:settings.studentAreaEnabled}}/></ProfessorShell>}
