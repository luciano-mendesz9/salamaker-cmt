"use client";
import Link from "next/link";import {useState} from "react";import {useRouter} from "next/navigation";import {ArrowLeft,Eye,KeyRound,LockKeyhole,LogIn} from "lucide-react";import {toast} from "sonner";import {Brand} from "@/components/brand";
export default function Login(){const router=useRouter();const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const form=new FormData(e.currentTarget);const response=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({accessCode:form.get("accessCode"),password:form.get("password")})});const data=await response.json();setLoading(false);if(!response.ok){toast.error(data.message);return}router.replace(data.redirectTo);router.refresh()}return <main className="mesh grid-pattern grid min-h-screen place-items-center px-5 py-10"><div className="w-full max-w-md"><div className="mb-8 flex justify-center"><Brand/></div><section className="glass rounded-3xl p-6 sm:p-8"><p className="eyebrow">Área segura</p><h1 className="mt-3 font-display text-3xl font-bold">Acesse sua conta</h1><p className="mt-2 text-sm text-slate-400">Use o código entregue pelo professor.</p><form onSubmit={submit} className="mt-7 space-y-5"><label className="block text-sm font-bold">Código de acesso<div className="relative mt-2"><KeyRound className="pointer-events-none absolute left-4 top-1/2 z-10 -translate-y-1/2 text-slate-500" size={18}/><input name="accessCode" className="input uppercase" style={{paddingLeft:"3rem"}} placeholder="AL2026001" autoComplete="username" required/></div></label><label className="block text-sm font-bold">Senha<div className="relative mt-2"><LockKeyhole className="pointer-events-none absolute left-4 top-1/2 z-10 -translate-y-1/2 text-slate-500" size={18}/><input name="password" type="password" className="input" style={{paddingLeft:"3rem",paddingRight:"3rem"}} placeholder="Sua senha" autoComplete="current-password" required/><Eye className="pointer-events-none absolute right-4 top-1/2 -translate-y-1/2 text-slate-500" size={18}/></div></label><button disabled={loading} className="button-primary w-full py-4"><LogIn size={19}/>{loading?"Entrando...":"Entrar"}</button></form><button onClick={()=>toast.info("Procure o professor para redefinir sua senha.")} className="mt-5 w-full text-center text-sm font-semibold text-blue-300 hover:text-blue-200">Esqueceu sua senha?</button></section><Link href="/" className="mx-auto mt-6 flex w-fit items-center gap-2 text-sm text-slate-400 hover:text-white"><ArrowLeft size={16}/>Voltar ao início</Link></div></main>}
