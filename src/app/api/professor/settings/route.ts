import {compare} from "bcryptjs";import {NextResponse} from "next/server";import {z} from "zod";import {prisma} from "@/lib/db";import {apiError,requireTeacher} from "@/lib/authorization";
const colors=["#328fff","#8b5cf6","#06b6d4","#10b981","#f59e0b","#f43f5e"] as const;const schema=z.discriminatedUnion("action",[z.object({action:z.literal("THEME"),accentColor:z.enum(colors)}),z.object({action:z.literal("STUDENT_AREA"),enabled:z.boolean(),teacherPassword:z.string().min(1)})]);
export async function GET(){try{await requireTeacher();const settings=await prisma.appSetting.upsert({where:{id:1},create:{id:1},update:{}});return NextResponse.json({settings})}catch(e){const[m,s]=apiError(e);return NextResponse.json({message:m},{status:s})}}
export async function PATCH(request:Request){try{const teacher=await requireTeacher();const body=schema.parse(await request.json());if(body.action==="STUDENT_AREA"&&!(await compare(body.teacherPassword,teacher.passwordHash)))return NextResponse.json({message:"Senha do professor incorreta."},{status:403});const data=body.action==="THEME"?{accentColor:body.accentColor}:{studentAreaEnabled:body.enabled};const settings=await prisma.$transaction(async tx=>{const updated=await tx.appSetting.upsert({where:{id:1},create:{id:1,...data},update:data});await tx.auditLog.create({data:{actorId:teacher.id,event:body.action==="THEME"?"THEME_CHANGED":"MODERATION_CHANGED",details:body.action==="THEME"?{accentColor:body.accentColor}:{studentAreaEnabled:body.enabled}}});return updated});return NextResponse.json({settings})}catch(e){if(e instanceof z.ZodError)return NextResponse.json({message:"Configuração inválida."},{status:400});const[m,s]=apiError(e);return NextResponse.json({message:m},{status:s})}}
