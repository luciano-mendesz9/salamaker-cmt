"use client";import {CheckCircle2,KeyRound} from "lucide-react";import {toast} from "sonner";
export function PresenceForm(){async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const response=await fetch("/api/presence",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({accessCode:f.get("accessCode"),lessonCode:f.get("lessonCode")})});const data=await response.json();if(response.ok)toast.success(data.message);else toast.error(data.message);if(response.ok)e.currentTarget.reset()}return <form onSubmit={submit} className="mt-6 space-y-5"><label className="block text-sm font-bold">Código do aluno<input name="accessCode" className="input mt-2 uppercase" placeholder="AL2026001" required/></label><label className="block text-sm font-bold">Código da aula<span className="relative mt-2 block"><input name="lessonCode" className="input pr-12 tracking-[.3em]" inputMode="numeric" pattern="[0-9]{6}" maxLength={6} placeholder="000000" required/><KeyRound className="absolute right-4 top-4 text-slate-500" size={19}/></span></label><button className="button-primary w-full py-4"><CheckCircle2 size={20}/>Confirmar presença</button></form>}
