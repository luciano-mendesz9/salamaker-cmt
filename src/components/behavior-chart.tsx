import {Activity} from "lucide-react";
export function BehaviorChart({ratings}:{ratings:{id:string;score:number;reason:string|null;createdAt:Date;lesson:{title:string}}[]}){if(!ratings.length)return <section className="glass mt-6 rounded-2xl p-5"><h2 className="flex items-center gap-2 font-display text-xl font-bold"><Activity size={20}/>Meu comportamento</h2><p className="mt-4 rounded-xl border border-dashed border-slate-700 p-6 text-center text-sm text-slate-500">Você ainda não recebeu avaliações de comportamento.</p></section>;const average=ratings.reduce((sum,r)=>sum+r.score,0)/ratings.length;return <section className="glass mt-6 rounded-2xl p-5"><div className="flex items-end justify-between gap-4"><div><h2 className="flex items-center gap-2 font-display text-xl font-bold"><Activity size={20}/>Meu comportamento</h2><p className="mt-1 text-sm text-slate-400">Evolução nas últimas aulas avaliadas.</p></div><div className="text-right"><strong className="font-display text-3xl text-blue-300">{average.toFixed(1)}</strong><p className="text-xs text-slate-500">média de 5</p></div></div><div className="mt-7 flex h-44 items-end gap-3 border-b border-slate-700 px-2">{ratings.slice().reverse().map(r=><div key={r.id} className="flex h-full flex-1 flex-col justify-end text-center" title={`${r.lesson.title}: ${r.score}/5`}><span className="mb-2 text-xs font-bold">{r.score}</span><div className={`mx-auto w-full max-w-12 rounded-t-lg ${r.score>=4?"bg-emerald-400":r.score===3?"bg-amber-400":"bg-rose-400"}`} style={{height:`${r.score*20}%`}}/><span className="mt-2 truncate text-[10px] text-slate-500">{r.lesson.title}</span></div>)}</div><div className="mt-5 space-y-2">{ratings.slice(0,3).map(r=><div key={r.id} className="flex justify-between gap-3 text-xs"><span className="text-slate-400">{r.lesson.title}{r.reason?` · ${r.reason}`:""}</span><strong>{r.score}/5</strong></div>)}</div></section>}
